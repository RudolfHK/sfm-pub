# Visualization dependencies (optional — only needed with --visualize)
# Install with: pip install -r requirements-viz.txt
matplotlib>=3.5
networkx>=2.8
imageio>=2.22
scipy>=1.9        # gaussian_filter for density heatmaps
Pillow>=9.0       # fallback image loader

# Optional: interactive 3-D viewer (open3d can be omitted on headless servers)
# open3d>=0.17
